CREATE FUNCTION genBoard(name varchar(100))
RETURNS NULL
BEGIN
    CREATE TABLE name(
        parentPost int,
        title varchar(200), 
        postId int NOT NULL AUTO_INCREMENT, 
        time TIMESTAMP, 
        content varchar(2000),
        PRIMARY KEY(postId)
    );
END
