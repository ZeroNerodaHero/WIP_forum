to delete u need to:
-delete from peepo[board] 
-delete table from peepoPost 


code for posts:
- (-1) means pinned
- (0) no parent
- (x) means child of 

scrap make a boardThread and threadId in mysql so when u create a thread u update boththings. only good solution. aka how will i do bumps with the first??

show tables like "shit\_%"

i need to set the title of the boards
i need to make redirects after every repost and remake the redirect page
